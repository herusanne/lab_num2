package com.lab1;
import java.util.ArrayList;
public class Prismas {
        public ArrayList<RightSquarePrism> list;
        public Prismas() {
            this.list = new ArrayList<>();
        }
        /*
    public double getMax()
    {
        Square[] squares;
        int maxIndex = 0;
        double max = 0;
        for (int i = 0; i < squares[i].length; i++) {
            if (squares[i].getArea() > max) {
                max = squares[i].getArea();
                maxIndex = i;
            }
        }
        return max;
    }
    public double getAverageArea() {
        double area = 0;
        for (final RightSquarePrism squares : square) {
            area += list.g;
        }
        return area / triangles.length;
    }
    */

}
